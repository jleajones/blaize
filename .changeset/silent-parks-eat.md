---
'@blaizejs/testing-utils': patch
---

Add assertion helpers to reduce test boilerplate:

- `logger.assertInfoCalled()` / `assertErrorCalled()` etc.
- `eventBus.assertPublished()` / `assertNotPublished()`
- New `createRouteTestContext()` helper
